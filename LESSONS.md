# LESSONS.md — 实战经验教训（本项目为什么这么设计）

本项目来自一次真实的批量测速实战：3 个机场（良心 41 节点 / 赔钱 57 节点 / 月兔 24 节点）、
98+ 节点、覆盖晚高峰整夜压测。以下是踩过的坑与结论 —— 每一条都直接变成了代码里的防失真措施。

## 1. speed.cloudflare.com 会限流单连接（最大的坑）

现象：所有节点单线程速度惊人一致地慢（1.6~1.7 MB/s），多线程却正常（24-40 MB/s），直连也慢。
逐秒采样显示：首秒 13-19 MB/s → 之后骤降到 0.01~0.05 并保持。

结论：CF 的测速文件（`speed.cloudflare.com/__down`）对单连接限流。**单线程速度是流媒体观看的关键指标，
被它一限就全废。** 对策：主测速源改用 Google CDN 大文件（`android-ndk-r21e`，1.1GB，实测单连接 380 Mbps 稳定）。

## 2. 小文件 + 持续下载 = 重连 = 触发机场连接限流

现象：50MB 文件在 40MB/s 下 1 秒下完，时长模式工具自动重连继续下 → 重连后的新连接被机场限速到 0.14 MB/s。
复现实验：`size_bytes=50MB` → 首秒 48MB/s 后 0.14 卡死；`size_bytes=500MB` → 全程 30-56MB/s 正常。

结论：时长模式必须把文件大小上限预留到 `duration × 预估速度`（本项目按 55MB/s 计算，上限 1000MB），
保证单连接全程不重连。**同一 URL、同一节点，测试代码写法不同结果能差 20 倍。**

## 3. mihomo 的 GLOBAL 默认是 DIRECT（选择陷阱）

现象：某轮实验"多代理配置测出 262 Mbps"，实际是 select 失败后流量**悄悄走了直连**。
后来在干净环境复现：`GET /proxies/GLOBAL` 在未选择时返回 `"now": "DIRECT"`。

结论：**节点切换后必须 API 回读验证**（`now == 目标节点`），验证失败宁可报错也不测——
"测错节点"比"测出慢"更危险，会得出完全相反的结论。

## 4. 并发测试会互相压低（自己人打自己人）

现象：两个 mihomo 实例同时测同一机场节点，一个测出 373 Mbps，另一个只有 1.14 MB/s —— 同节点同时刻。

结论：同一节点同时只允许一个测试；精测必须串行；快扫模式（并发）的数字要标注"分摊带宽，仅供参考"。
启动前检测机器上的 mihomo 实例数并告警。

## 5. 晚高峰 vs 非高峰：数据不可直接对比

现象：同一机场同一节点，21:00 直连 19.7 MB/s，21:33 直连 1.8 MB/s，22:00 又 33 MB/s。
月兔机场更极端：同一节点 23:39 测 380 Mbps，23:41 测 10 Mbps，00:05 又 390 Mbps（分钟级波动）。

结论：测试报告必须带**时段标注**；评估机场请用 `--loop` 多轮覆盖晚高峰；
单轮数据只能代表"那一刻"，不能代表"那个机场"。

## 6. fast.com 已无法自动化（2025 起）

现象：`api.fast.com/netflix/speedtest/v2` 与 `api-global.netflix.com/oca/speedtest`，
新旧 token、各种请求头、直连/代理全部 403「Not Available」。

结论：Netflix 封死了自动化。等效替代：**Ookla 引擎**（speedtest.net 网页同款）。
trevor.speedtestcustom.com 逆向确认 = 官方 Ookla `speedtest-js-engine`，服务器列表走
`{subdomain}.speedtestcustom.com/api/js/servers`（返回全球 Ookla 服务器），与 speedtest.net 同源。

## 7. 环境类坑（Windows）

- **curl.exe 在沙箱里被拒**（输出经管道捕获即被拦）→ 用 PowerShell 重定向到文件，或直接用 Node/Python 的 HTTP 栈。
- **Windows Schannel TLS 损坏** → PowerShell 的 .NET HTTPS 全挂（"安全包中没有可用的凭证"），Node（自带 OpenSSL）正常。
- **系统代理指向已死的 127.0.0.1:7897** → .NET 的 WebRequest 全走死代理失败；绕过系统代理直连即可。
- **mihomo 用 `stdio:'ignore'` 启动会崩**（0xC0000142）→ stdout/stderr 重定向到文件。
- **mihomo 参数是 `-f` 不是 `-c`**。

## 8. 测试方法学（防失真清单）

1. 单线程 = 流媒体真实体验（Emby 直连播放是单连接），多线程 = 节点最大吞吐，两者都要报。
2. 每秒采样落盘（JSON），任何结论可复核；报告给出数据质量评级（A/B/C）。
3. 丢包 = 多次探测失败率；断流 = 持续下载中 <1MB/s 的秒数（排除首秒慢启动）。
4. "全节点速度异常均匀"是限流特征，自动识别并告警。
5. 直连基线快于全部节点 → 节点选择可能未生效，自动告警。
6. 深度测试（Ookla/trevor）的服务器按距离选择（中国出口通常分到越南/周边），
   数值受服务器位置影响，用于横向对比节点，不与 Google CDN 单线速度直接比较。

## 9. 结果速览（2026-08-15 实测，供参考）

- 良心（liangxin.xyz）：41 节点 23 可用；头部 日本高速02 / 美国高速04 / 香港03住宅IP，单线程 25-31 MB/s，0 断流。
- 赔钱（dasho.pqjc.site）：57 节点 32 可用；头部 日本东京08（最低 7.6MB/s 最稳）/ 美国05 / 美国04，单线程 26-30 MB/s。
- 月兔（yuetu）：24 节点 23 可用；峰值 390 Mbps 但**分钟级波动**（1~49 MB/s），高峰期不可靠，仅适合备用。
- Emby 评估线：≥50MB/s 4K原盘 ｜ ≥30 4K高码率 ｜ ≥15 1080p高码/4K低码 ｜ ≥8 1080p ｜ <8 不推荐。
